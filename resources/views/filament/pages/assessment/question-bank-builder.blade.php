<x-filament-panels::page><div class="space-y-6">@if(session('exam_notice'))<div class="rounded-xl bg-success-50 p-4 text-success-800">{{ session('exam_notice') }}</div>@endif @unless($this->schemaReady())<div class="rounded-xl bg-warning-50 p-5">Migration ujian online belum dijalankan. Jalankan melalui prosedur deploy staging.</div>@else<section class="rounded-2xl border border-primary-200 bg-gradient-to-br from-primary-50 via-white to-amber-50 p-6"><h2 class="text-2xl font-bold">Ruang Kerja Soal</h2><p class="mt-2 text-sm">Kunci tersimpan di server dan tidak pernah masuk payload ujian siswa.</p><form method="post" action="{{ route('admin.exam.question-sets.store') }}" class="mt-5 grid gap-3 md:grid-cols-3">@csrf<input class="fi-input" name="title" placeholder="Judul paket" required><input class="fi-input" name="subject" placeholder="Mata pelajaran" required><select class="fi-input" name="exam_type">@foreach(['ASTS','ASAS','ASAT','Lainnya'] as $v)<option>{{ $v }}</option>@endforeach</select><input class="fi-input" name="academic_year" placeholder="2026/2027" required><select class="fi-input" name="semester"><option>Ganjil</option><option>Genap</option></select><textarea class="fi-input" name="instructions" placeholder="Petunjuk siswa"></textarea><button class="fi-btn bg-primary-600 px-4 py-2 text-white">Buat Paket</button></form></section>@foreach($this->questionSets() as $set)<section class="rounded-2xl border bg-white p-5 dark:bg-gray-900"><div class="flex flex-wrap justify-between gap-3"><div><h3 class="text-lg font-bold">{{ $set->title }}</h3><p class="text-sm">{{ $set->subject }} · {{ $set->exam_type }} · {{ $set->questions->count() }} soal · {{ $set->status }}</p></div><div class="flex gap-2"><a class="fi-btn bg-gray-800 px-3 py-2 text-white" target="_blank" href="{{ route('admin.exam.question-sets.preview',[$set,'student']) }}">PDF Siswa</a><a class="fi-btn bg-amber-600 px-3 py-2 text-white" target="_blank" href="{{ route('admin.exam.question-sets.preview',[$set,'teacher']) }}">PDF Kunci</a></div></div><details class="mt-4"><summary class="cursor-pointer font-bold">Tambah soal</summary><form method="post" action="{{ route('admin.exam.question-sets.questions.store',$set) }}" class="mt-3 grid gap-3 md:grid-cols-2">@csrf<select class="fi-input" name="type"><option value="multiple_choice">Pilihan Ganda</option><option value="multiple_response">PG Kompleks</option><option value="true_false">Benar/Salah</option><option value="essay">Essay</option></select><select class="fi-input" name="cognitive_level"><option>HOTS</option><option selected>MOTS</option><option>LOTS</option></select><textarea class="fi-input md:col-span-2" name="prompt" placeholder="Teks soal" required></textarea><textarea class="fi-input" name="options_text" placeholder="Opsi, satu per baris"></textarea><textarea class="fi-input" name="answer_key_text" placeholder="Kunci; PG kompleks satu per baris"></textarea><input class="fi-input" type="number" step=".01" name="weight" value="1" required><textarea class="fi-input" name="explanation" placeholder="Pembahasan"></textarea><textarea class="fi-input" name="rubric" placeholder="Rubrik essay"></textarea><button class="fi-btn bg-primary-600 px-4 py-2 text-white">Simpan Soal</button></form></details><ol class="mt-4 list-decimal space-y-2 pl-5">@foreach($set->questions as $q)<li>{{ \Illuminate\Support\Str::limit($q->prompt,120) }} <span class="text-xs">({{ $q->type }}, {{ $q->weight }})</span></li>@endforeach</ol></section>@endforeach @if($this->canPrepareAdvancedFeatures())<section class="rounded-2xl border bg-white p-5 dark:bg-gray-900"><h2 class="text-lg font-bold">Pengaturan AI Soal</h2><p class="text-sm">API key: {{ $this->aiSetting()?->maskedKey() ?? 'Belum diatur' }}. Nilai lama dipertahankan bila kolom key dikosongkan.</p><form method="post" action="{{ route('admin.exam.ai.save') }}" class="mt-3 grid gap-3 md:grid-cols-2">@csrf<input class="fi-input" name="provider" value="{{ $this->aiSetting()?->provider ?? 'openai-compatible' }}" required><input class="fi-input" type="url" name="base_url" value="{{ $this->aiSetting()?->base_url }}" placeholder="https://provider.example/v1"><input class="fi-input" name="model" value="{{ $this->aiSetting()?->model }}" placeholder="Model"><input class="fi-input" type="password" name="api_key" placeholder="Kosongkan untuk mempertahankan key"><input class="fi-input" type="number" name="daily_limit" value="{{ $this->aiSetting()?->daily_limit ?? 0 }}"><input class="fi-input" type="number" name="per_request_limit" value="{{ $this->aiSetting()?->per_request_limit ?? 10 }}"><label><input type="checkbox" name="enabled" value="1" @checked($this->aiSetting()?->enabled)> Aktif</label><button class="fi-btn bg-primary-600 px-4 py-2 text-white">Simpan AI</button></form><form method="post" action="{{ route('admin.exam.ai.test') }}">@csrf<button class="mt-3 fi-btn bg-gray-700 px-4 py-2 text-white">Test kesiapan (tanpa panggilan jaringan)</button></form></section>@endif @endunless</div></x-filament-panels::page>
