<x-filament-panels::page><div class="space-y-6">@if(session('exam_notice'))<div class="rounded-xl bg-success-50 p-4">{{ session('exam_notice') }} @if(session('exam_student_token'))<strong class="ml-2 rounded bg-white px-2 py-1 font-mono text-base">{{ session('exam_student_token') }}</strong>@endif</div>@endif @unless($this->schemaReady())<div class="rounded-xl bg-warning-50 p-5">Migration ujian online belum tersedia.</div>@else<section class="rounded-2xl border bg-white p-5 dark:bg-gray-900"><h2 class="text-xl font-bold">Buat Jadwal Ujian</h2><form class="mt-4 grid gap-3 md:grid-cols-3" method="post" action="{{ route('admin.exam.schedules.store') }}">@csrf<select class="fi-input" name="question_set_id">@foreach($this->sets() as $set)<option value="{{ $set->id }}">{{ $set->title }}</option>@endforeach</select><input class="fi-input" name="class_name" placeholder="Kelas / rombel" required><input class="fi-input" name="exam_code" placeholder="Kode ujian" required><input class="fi-input" type="password" name="supervisor_code" placeholder="Kode pengawas (min. 4)" required><input class="fi-input" type="datetime-local" name="starts_at" required><input class="fi-input" type="datetime-local" name="ends_at" required><input class="fi-input" type="number" name="duration_minutes" value="90" required><button class="fi-btn bg-primary-600 px-4 py-2 text-white">Buat Jadwal</button></form></section>@foreach($this->schedules() as $schedule)<section class="rounded-2xl border bg-white p-5 dark:bg-gray-900"><h3 class="font-bold">{{ $schedule->questionSet->title }} · {{ $schedule->class_name }}</h3><p class="text-sm">Kode: {{ $schedule->exam_code }} | Terverifikasi {{ $schedule->tokens->whereNotNull('verified_at')->count() }}/{{ $schedule->tokens->count() }} | Mulai {{ $schedule->attempts->whereNotNull('started_at')->count() }} | Submit {{ $schedule->attempts->where('status','submitted')->count() }}</p><details class="mt-3"><summary>Tambah peserta</summary><form class="mt-3 grid gap-2 md:grid-cols-4" method="post" action="{{ route('admin.exam.schedules.students.store',$schedule) }}">@csrf<input class="fi-input" name="student_name" placeholder="Nama" required><input class="fi-input" name="nisn" placeholder="NISN" required><input class="fi-input" type="date" name="birth_date" required><input class="fi-input" name="class_name" value="{{ $schedule->class_name }}" required><button class="fi-btn bg-primary-600 px-3 py-2 text-white">Tambah</button></form></details></section>@endforeach<section class="rounded-2xl border bg-white p-5 dark:bg-gray-900"><h2 class="text-xl font-bold">Hasil & Koreksi Ujian</h2><p class="text-sm">Nilai final adalah nilai murni ujian. Tombol “Gunakan sebagai Nilai Rapor” belum diaktifkan agar tidak menimpa rapor tanpa audit mapping.</p><div class="mt-4 space-y-4">@forelse($this->attempts() as $attempt)<article class="rounded-xl border p-4"><b>{{ $attempt->studentToken->student_name }} — {{ $attempt->schedule->questionSet->title }}</b><p class="text-sm">Status {{ $attempt->status }} | Terjawab {{ $attempt->answers->count() }} | Keluar {{ $attempt->exit_count }} | Offline {{ $attempt->offline_count }} | Auto {{ $attempt->auto_score }} | Essay {{ $attempt->essay_score }} | Final {{ $attempt->final_score ?? 'menunggu' }}</p>@foreach($attempt->answers->where('question.type','essay') as $answer)<form class="mt-2 flex flex-wrap gap-2" method="post" action="{{ route('admin.exam.answers.grade',$answer) }}">@csrf <span class="text-sm">{{ \Illuminate\Support\Str::limit($answer->question->prompt,60) }}</span><input class="fi-input w-24" type="number" step=".01" max="{{ $answer->question->weight }}" name="manual_score" value="{{ $answer->manual_score }}" required><input class="fi-input" name="teacher_feedback" value="{{ $answer->teacher_feedback }}" placeholder="Catatan"><button class="fi-btn bg-primary-600 px-3 py-2 text-white">Simpan</button></form>@endforeach<button disabled class="mt-3 cursor-not-allowed rounded bg-gray-200 px-3 py-2 text-gray-500">Gunakan sebagai Nilai Rapor (belum aktif)</button></article>@empty<p>Belum ada attempt.</p>@endforelse</div></section>@endunless</div></x-filament-panels::page>
